#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_eschecker@0.0.5@eschecker/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../_eschecker@0.0.5@eschecker/index.js" "$@"
  ret=$?
fi
exit $ret
