"use strict";

var _sinon = require("sinon");

var _sinon2 = _interopRequireDefault(_sinon);

var _chai = require("chai");

var _babelTypes = require("babel-types");

var t = _interopRequireWildcard(_babelTypes);

var _program = require("../../../lib/semantic-model/program");

var _hindleyMilnerContext = require("../../../lib/type-inference/hindley-milner-context");

var _typeInferenceContext = require("../../../lib/type-inference/type-inference-context");

var _types = require("../../../lib/semantic-model/types");

var _conditionalExpressionRefinementRule = require("../../../lib/type-inference/refinement-rules/conditional-expression-refinement-rule");

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe("ConditionalExpressionRefinementRule", function () {
	let context, rule, program;

	beforeEach(function () {
		program = new _program.Program();
		context = new _hindleyMilnerContext.HindleyMilnerContext(null, new _typeInferenceContext.TypeInferenceContext(program));
		rule = new _conditionalExpressionRefinementRule.ConditionalExpressionRefinementRule();
	});

	describe("canRefine", function () {
		it("returns true if the node is a conditionalExpression", function () {
			// arrange
			const conditionalExpr = t.conditionalExpression(t.identifier("x"), t.identifier("x"), t.numericLiteral(0));

			// act
			(0, _chai.expect)(rule.canRefine(conditionalExpr)).to.be.true;
		});

		it("returns false if the node is not a conditional expression", function () {
			(0, _chai.expect)(rule.canRefine(t.identifier("x"))).to.be.false;
		});
	});

	describe("refine", function () {
		it("returns the unified type of the consequent and alternate branch", function () {
			// arrange
			const conditionalExpr = t.conditionalExpression(t.identifier("x"), t.identifier("x"), t.numericLiteral(0));

			_sinon2.default.stub(context, "infer").withArgs(conditionalExpr.consequent).returns(_types.MaybeType.of(_types.NumberType.create())).withArgs(conditionalExpr.alternate).returns(_types.NumberType.create());

			_sinon2.default.stub(context, "unify").returns(_types.MaybeType.of(_types.NumberType.create()));

			// act, assert
			(0, _chai.expect)(rule.refine(conditionalExpr, context)).to.be.instanceOf(_types.MaybeType);
			_sinon2.default.assert.calledWith(context.unify, _sinon2.default.match.instanceOf(_types.MaybeType), _sinon2.default.match.instanceOf(_types.NumberType));
		});

		it("infers the type of the test condition", function () {
			// arrange
			const conditionalExpr = t.conditionalExpression(t.identifier("x"), t.identifier("x"), t.numericLiteral(0));

			_sinon2.default.stub(context, "infer").returns(_types.NumberType.create());
			_sinon2.default.stub(context, "unify").returns(_types.NumberType.create());

			// act
			rule.refine(conditionalExpr, context);

			// assert
			_sinon2.default.assert.calledWith(context.infer, conditionalExpr.test);
		});
	});
});
//# sourceMappingURL=conditional-expression-refinement-rule.spec.js.map
