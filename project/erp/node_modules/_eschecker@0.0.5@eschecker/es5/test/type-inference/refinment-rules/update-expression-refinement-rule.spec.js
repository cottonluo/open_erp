"use strict";

var _sinon = require("sinon");

var _sinon2 = _interopRequireDefault(_sinon);

var _chai = require("chai");

var _babelTypes = require("babel-types");

var t = _interopRequireWildcard(_babelTypes);

var _program = require("../../../lib/semantic-model/program");

var _hindleyMilnerContext = require("../../../lib/type-inference/hindley-milner-context");

var _typeInferenceContext = require("../../../lib/type-inference/type-inference-context");

var _updateExpressionRefinementRule = require("../../../lib/type-inference/refinement-rules/update-expression-refinement-rule");

var _types = require("../../../lib/semantic-model/types");

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe("UpdateExpressionRefinementRule", function () {
	let context, rule, program;

	beforeEach(function () {
		program = new _program.Program();
		context = new _hindleyMilnerContext.HindleyMilnerContext(null, new _typeInferenceContext.TypeInferenceContext(program));
		rule = new _updateExpressionRefinementRule.UpdateExpressionRefinementRule();
	});

	describe("canRefine", function () {
		it("returns true if the node is an update expression", function () {
			// arrange
			const updateExpression = t.updateExpression("++", t.identifier("x"));

			// act, assert
			(0, _chai.expect)(rule.canRefine(updateExpression)).to.be.true;
		});

		it("returns false if the node is not an update expression", function () {
			(0, _chai.expect)(rule.canRefine(t.identifier("x"))).to.be.false;
		});
	});

	describe("refine", function () {
		it("returns number type", function () {
			// arrange
			const updateExpression = t.updateExpression("++", t.identifier("x"));

			_sinon2.default.stub(context, "infer").returns(_types.NumberType.create());
			_sinon2.default.stub(context, "unify").returns(_types.NumberType.create());

			// act, assert
			(0, _chai.expect)(rule.refine(updateExpression, context)).to.be.instanceOf(_types.NumberType);
		});

		it("infers and unifies the type of the argument with the maybe number type", function () {
			// arrange
			const updateExpression = t.updateExpression("++", t.identifier("x"));

			_sinon2.default.stub(context, "infer").returns(_types.NumberType.create());
			_sinon2.default.stub(context, "unify").returns(_types.NumberType.create());

			// act
			rule.refine(updateExpression, context);

			// assert
			_sinon2.default.assert.calledWith(context.infer, updateExpression.argument);
			_sinon2.default.assert.calledWith(context.unify, _sinon2.default.match.instanceOf(_types.MaybeType), _sinon2.default.match.instanceOf(_types.NumberType), updateExpression.argument);
		});
	});
});
//# sourceMappingURL=update-expression-refinement-rule.spec.js.map
