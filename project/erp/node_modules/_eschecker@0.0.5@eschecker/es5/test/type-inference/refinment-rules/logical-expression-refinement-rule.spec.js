"use strict";

var _sinon = require("sinon");

var _sinon2 = _interopRequireDefault(_sinon);

var _chai = require("chai");

var _babelTypes = require("babel-types");

var t = _interopRequireWildcard(_babelTypes);

var _program = require("../../../lib/semantic-model/program");

var _hindleyMilnerContext = require("../../../lib/type-inference/hindley-milner-context");

var _typeInferenceContext = require("../../../lib/type-inference/type-inference-context");

var _types = require("../../../lib/semantic-model/types");

var _logicalExpressionRefinementRule = require("../../../lib/type-inference/refinement-rules/logical-expression-refinement-rule");

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe("LogicalExpressionRefinementRule", function () {
	let context, rule, program;

	beforeEach(function () {
		program = new _program.Program();
		context = new _hindleyMilnerContext.HindleyMilnerContext(null, new _typeInferenceContext.TypeInferenceContext(program));
		rule = new _logicalExpressionRefinementRule.LogicalExpressionRefinementRule();
	});

	describe("canRefine", function () {
		it("returns true if the node is a logical expression", function () {
			// arrange
			const logicalExpression = t.logicalExpression("&&", t.identifier("x"), t.identifier("x"));

			// act
			(0, _chai.expect)(rule.canRefine(logicalExpression)).to.be.true;
		});

		it("returns false if the node is not a logical expression", function () {
			(0, _chai.expect)(rule.canRefine(t.identifier("x"))).to.be.false;
		});
	});

	describe("refine", function () {
		it("returns the boolean type", function () {
			// arrange
			const logicalExpression = t.logicalExpression("&&", t.identifier("x"), t.identifier("x"));

			_sinon2.default.stub(context, "infer").returns(_types.NumberType.create());

			// act, assert
			(0, _chai.expect)(rule.refine(logicalExpression, context)).to.be.instanceOf(_types.BooleanType);
		});

		it("infers the type of the test left and right hand side of the expression", function () {
			// arrange
			const logicalExpression = t.logicalExpression("&&", t.identifier("x"), t.identifier("x"));

			_sinon2.default.stub(context, "infer").returns(_types.NumberType.create());

			// act
			rule.refine(logicalExpression, context);

			// assert
			_sinon2.default.assert.calledWith(context.infer, logicalExpression.left);
			_sinon2.default.assert.calledWith(context.infer, logicalExpression.right);
		});
	});
});
//# sourceMappingURL=logical-expression-refinement-rule.spec.js.map
