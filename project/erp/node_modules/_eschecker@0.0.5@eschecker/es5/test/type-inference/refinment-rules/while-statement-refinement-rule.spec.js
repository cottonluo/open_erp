"use strict";

var _sinon = require("sinon");

var _sinon2 = _interopRequireDefault(_sinon);

var _chai = require("chai");

var _babelTypes = require("babel-types");

var t = _interopRequireWildcard(_babelTypes);

var _program = require("../../../lib/semantic-model/program");

var _hindleyMilnerContext = require("../../../lib/type-inference/hindley-milner-context");

var _typeInferenceContext = require("../../../lib/type-inference/type-inference-context");

var _whileStatementRefinementRule = require("../../../lib/type-inference/refinement-rules/while-statement-refinement-rule");

var _types = require("../../../lib/semantic-model/types");

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe("WhileStatementRefinementRule", function () {
	let context, rule, program;

	beforeEach(function () {
		program = new _program.Program();
		context = new _hindleyMilnerContext.HindleyMilnerContext(null, new _typeInferenceContext.TypeInferenceContext(program));
		rule = new _whileStatementRefinementRule.WhileStatementRefinementRule();
	});

	describe("canRefine", function () {
		it("returns true if the node is a while statement", function () {
			// arrange
			const whileStatement = t.whileStatement(t.booleanLiteral(true), t.blockStatement([]));

			// act, assert
			(0, _chai.expect)(rule.canRefine(whileStatement)).to.be.true;
		});

		it("returns true if the node is a do while statement", function () {
			// arrange
			const doWhileStatement = t.doWhileStatement(t.booleanLiteral(true), t.blockStatement([]));

			// act, assert
			(0, _chai.expect)(rule.canRefine(doWhileStatement)).to.be.true;
		});

		it("returns false if the node is neither a while nor do while statement", function () {
			(0, _chai.expect)(rule.canRefine(t.identifier("x"))).to.be.false;
		});
	});

	describe("refine", function () {
		it("returns type void", function () {
			// arrange
			const whileStatement = t.whileStatement(t.booleanLiteral(true), t.blockStatement([]));

			_sinon2.default.stub(context, "infer").returns(_types.BooleanType.create());

			// act, assert
			(0, _chai.expect)(rule.refine(whileStatement, context)).to.be.instanceOf(_types.VoidType);
		});

		it("infers the test expression", function () {
			// arrange
			const whileStatement = t.whileStatement(t.booleanLiteral(true), t.blockStatement([]));

			_sinon2.default.stub(context, "infer").returns(_types.BooleanType.create());

			// assert
			rule.refine(whileStatement, context);

			// assert
			_sinon2.default.assert.calledWith(context.infer, whileStatement.test);
		});
	});
});
//# sourceMappingURL=while-statement-refinement-rule.spec.js.map
