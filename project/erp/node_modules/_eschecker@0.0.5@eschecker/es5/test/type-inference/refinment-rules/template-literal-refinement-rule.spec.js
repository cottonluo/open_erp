"use strict";

var _sinon = require("sinon");

var _sinon2 = _interopRequireDefault(_sinon);

var _chai = require("chai");

var _babelTypes = require("babel-types");

var t = _interopRequireWildcard(_babelTypes);

var _program = require("../../../lib/semantic-model/program");

var _hindleyMilnerContext = require("../../../lib/type-inference/hindley-milner-context");

var _typeInferenceContext = require("../../../lib/type-inference/type-inference-context");

var _templateLiteralRefinementRule = require("../../../lib/type-inference/refinement-rules/template-literal-refinement-rule");

var _types = require("../../../lib/semantic-model/types");

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe("TemplateLiteralRefinementRule", function () {
	let context, rule, templateLiteral;

	beforeEach(function () {
		context = new _hindleyMilnerContext.HindleyMilnerContext(null, new _typeInferenceContext.TypeInferenceContext(new _program.Program()));
		rule = new _templateLiteralRefinementRule.TemplateLiteralRefinementRule();
		templateLiteral = t.templateLiteral([t.templateElement("Hello"), t.templateElement("")], [t.identifier("user")]); // Hello ${user}
	});

	describe("canRefine", function () {
		it("returns true if the node is a template literal", function () {
			(0, _chai.expect)(rule.canRefine(templateLiteral)).to.be.true;
		});

		it("returns false if the node is not a template literal", function () {
			(0, _chai.expect)(rule.canRefine(t.identifier("x"))).to.be.false;
		});
	});

	describe("refine", function () {
		it("returns type string", function () {
			// arrange
			_sinon2.default.stub(context, "infer").returns(_types.NumberType.create());

			// act, assert
			(0, _chai.expect)(rule.refine(templateLiteral, context)).to.be.instanceOf(_types.StringType);
		});

		it("infers the type of the expressions", function () {
			// arrange
			_sinon2.default.stub(context, "infer").returns(_types.StringType.create());

			// assert
			rule.refine(templateLiteral, context);

			// assert
			_sinon2.default.assert.calledWith(context.infer, templateLiteral.expressions[0]);
		});
	});
});
//# sourceMappingURL=template-literal-refinement-rule.spec.js.map
